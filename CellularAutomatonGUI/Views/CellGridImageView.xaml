<UserControl
    x:Class="CellularAutomatonGUI.Views.CellGridImageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cag="clr-namespace:CellularAutomatonGUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer>
        <ScrollViewer.Resources>
            <cag:RelativePathStringToImageSourceConverter x:Key="relativePathStringToImageSourceConverter" />
        </ScrollViewer.Resources>
        <Image
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Source="{Binding Path=BitmapImage, Mode=OneWay}"
            Stretch="None" />
    </ScrollViewer>
</UserControl>