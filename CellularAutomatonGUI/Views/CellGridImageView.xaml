<UserControl x:Class="CellularAutomatonGUI.Views.CellGridImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cag="clr-namespace:CellularAutomatonGUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <ScrollViewer.Resources>
            <cag:RelativePathStringToImageSourceConverter x:Key="relativePathStringToImageSourceConverter"/>
        </ScrollViewer.Resources>
        <Image VerticalAlignment="Top" HorizontalAlignment="Center" Stretch="None"
               Source="{Binding Path=CellGridImageFilename, 
                                Mode=OneWay, 
                                Converter={StaticResource relativePathStringToImageSourceConverter}}"/>
    </ScrollViewer>
</UserControl>
